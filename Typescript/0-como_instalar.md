```bash
npm i -D typescript 			// instala o typescript
npx tsc --init 					// Cria um arquivo de configuração typescript
npm i ts-node-dev -D 						     // instala o ts-node-dev
ts-node-dev --respawn server.ts // script pra rodar
npm run dev 					// comando para rodar o programa
```